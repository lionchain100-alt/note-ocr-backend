# Note OCR Backend - MVP 需求文档（收口版）

> 目标：围绕 **手写笔记识别** 做一个最小可用后端闭环。后续开发默认以本文件为准，不额外扩展范围。

---

## 1. MVP 目标

为手写笔记场景提供一个 **可稳定调用的 OCR 后端服务**，支持：

- **手机端识别**
- **网页端识别**
- **上传图片识别**
- **拍照识别 / 摄像头拍照识别**

并输出 **尽可能保持原手写笔记排版结构** 的识别结果，而不是只返回一段打散后的纯文本。

---

## 2. 核心用户场景

### 场景 1：网页端上传图片识别
用户在网页端上传一张手写笔记照片，系统返回与原笔记排版接近的结构化文本结果。

### 场景 2：手机端上传相册图片识别
用户在手机网页或 App 内上传一张拍好的笔记照片，系统返回结构化识别结果。

### 场景 3：手机端直接拍照识别
用户通过手机摄像头直接拍摄一张手写笔记，提交后立即返回结构化识别结果。

### 场景 4：网页端摄像头拍照识别
用户在支持摄像头的网页端（桌面浏览器或移动浏览器）调用摄像头拍照后识别。

---

## 3. MVP 要解决的核心问题

MVP 第一阶段只解决以下问题：

1. 能接收手写笔记图片
2. 能识别图片中的手写文字
3. 能尽量保持原笔记的阅读顺序和排版结构
4. 能让手机端和网页端都方便调用
5. 能输出前端可直接消费的结构化结果

---

## 4. MVP 核心闭环

### 输入
支持单张笔记图片输入，来源包括：
- 文件上传
- 手机拍照上传
- 摄像头拍照后上传
- Base64 图片数据

### 处理
- 调用 OCR 引擎识别手写内容
- 基于坐标重建阅读顺序
- 尽量保留原笔记的段落、换行、相对布局
- 转换为结构化文本结果

### 输出
返回结构化 JSON，至少包含：
- `text`
- `markdown`
- `blocks`（推荐，表示结构块）
- `raw`（调试辅助，可保留）

---

## 5. MVP 必须功能（P0）

## 5.1 手写 OCR 能力
### 要求
- 以 **手写笔记识别** 为主
- 默认 OCR 引擎使用腾讯云手写 OCR
- 输入一张手写笔记图片，能返回识别结果

#### 当前状态
- [x] 腾讯云 OCR 已接入
- [x] 已支持手写识别主路径

---

## 5.2 单图识别 API
### 要求
提供稳定的单图识别接口，供网页端和手机端共用。

### 必须满足
- 提供 `POST /ocr`
- 支持单张图片输入
- 支持 Base64 图片
- 响应结构稳定

#### 当前状态
- [x] `POST /ocr` 已有
- [x] 已支持基础图片输入
- [ ] 需明确统一请求结构与错误返回

---

## 5.3 手机端 / 网页端通用接入
### 要求
后端必须对前端形态无感，支持：
- 网页上传
- 手机上传
- 摄像头拍照后上传

### 说明
“手机端拍照识别”和“网页端摄像头拍照识别”在 MVP 阶段，后端不直接控制摄像头，而是：
- 前端负责拍照
- 后端负责接收拍好的图片并识别

### 必须满足
- 后端接口不依赖特定前端容器
- 图片输入格式对手机端和网页端一致可用

#### 当前状态
- [x] HTTP 接口已具备前后端通用基础
- [ ] 需在文档中明确移动端/网页端输入规范

---

## 5.4 排版重建
### 要求
识别结果不能只是简单拼接纯文本，必须尽量保持原手写笔记的排版结构。

### 必须满足
- 基于坐标重建阅读顺序
- 支持基础换行保留
- 支持段落级结构重建
- 输出比 OCR 原始结果更接近原始笔记布局

### MVP 不要求
- 100% 视觉还原
- 精准复刻每个字的空间坐标样式
- 富文本编辑级别排版还原

#### 当前状态
- [x] 已有坐标排序算法
- [ ] 还需把“排版一致性”明确成验收标准
- [ ] 还需补 `blocks` 级输出设计（推荐纳入 MVP）

---

## 5.5 Markdown 输出
### 要求
必须输出适合笔记消费的 Markdown，而不是只给一段原始纯文本。

### 必须满足
- 返回 `markdown`
- Markdown 体现换行、段落、列表等基础结构
- 与原始笔记排版尽量一致

#### 当前状态
- [x] 已有 Markdown 转换逻辑
- [ ] 需明确 Markdown 输出规范

---

## 5.6 结构化块输出（推荐纳入 MVP）
### 要求
为了让前端更容易还原笔记排版，建议 MVP 直接输出 `blocks`。

### 输出示例
```json
{
  "blocks": [
    {
      "type": "line",
      "text": "今天完成 OCR 接口",
      "bbox": [10, 20, 300, 45]
    }
  ]
}
```

### 价值
- 前端可以自行渲染
- 更适合手机端/网页端布局恢复
- 比单纯 markdown 更适合后续扩展

#### 当前状态
- [ ] 尚未明确落地，建议纳入 MVP

---

## 5.7 健康检查
### 要求
提供 `GET /health` 用于部署检查。

#### 当前状态
- [x] 已具备

---

## 5.8 基础错误处理
### 要求
- 缺少 image 参数时返回明确错误
- OCR 调用失败时返回明确错误
- 非法图片输入时返回明确错误
- 服务异常时不能直接崩溃

#### 当前状态
- [ ] 需要补齐统一错误返回结构

---

## 5.9 稳定部署（MVP 阶段）
### 要求
MVP 先保证一套稳定部署模式，不同时追求多部署形态。

### 结论
- **Express 服务作为 MVP 主部署方式**
- Cloudflare Worker 不纳入 MVP 必须验收

#### 当前状态
- [x] Express 方案已存在
- [ ] Worker 相关内容降级为 MVP 之外

---

## 6. API 需求

## 6.1 POST /ocr
### 请求体（MVP 统一）
```json
{
  "image": "data:image/png;base64,...",
  "provider": "tencent"
}
```

### 响应体（MVP 要求）
```json
{
  "text": "识别文本",
  "markdown": "整理后的 Markdown",
  "blocks": [],
  "raw": {}
}
```

### 错误响应（建议统一）
```json
{
  "error": "INVALID_IMAGE",
  "message": "image is required"
}
```

---

## 6.2 GET /health
### 响应
```json
{ "status": "ok" }
```

---

## 7. MVP 验收标准

只有满足以下标准，才算 MVP 完成：

### 验收 1：网页端上传图片可识别
网页端上传一张手写笔记图片，接口返回成功结果。

### 验收 2：手机端上传图片可识别
手机端上传一张笔记图片，接口返回成功结果。

### 验收 3：拍照后上传可识别
无论来自手机摄像头还是网页摄像头，拍照后生成的图片都能被接口正确处理。

### 验收 4：结果排版接近原笔记
结果不是简单乱序拼接，而是能体现：
- 正确阅读顺序
- 基础换行
- 基础段落结构

### 验收 5：返回 Markdown + blocks
结果至少同时包含：
- `markdown`
- `blocks`

### 验收 6：异常路径可用
无效输入、OCR 失败时，接口能返回明确错误，不直接崩溃。

### 验收 7：Express 版可部署
Express 服务可稳定运行，并通过 `/health` 检查。

---

## 8. MVP 明确不做

以下内容不属于当前 MVP：

- Google Vision 作为正式主路径
- 多 OCR 引擎切换
- Cloudflare Worker 作为主部署形态
- Notion 集成
- 批量处理
- Redis 缓存
- 用户认证
- 使用统计
- 图片增强/旋转/裁剪
- 多租户 / 权限体系
- embedding / 向量检索类能力

#### 当前状态
- [x] 已明确排除这些扩展项

---

## 9. 上线前必须处理事项

这些不是 MVP 功能，但属于上线阻断项：

### 安全
- [ ] 轮换腾讯云密钥
- [ ] 确认 `.env` 不再泄露
- [ ] 清理历史暴露风险

### 稳定性
- [ ] 增加统一错误返回结构
- [ ] 增加基础日志
- [ ] 增加超时/异常保护

---

## 10. 后续开发规则

从现在开始，后续开发必须遵守：

1. 先对照本需求文档
2. 只补需求内未完成项
3. 不因为“还能扩展”就加入 P1/P2 能力
4. 新功能若不在本文档内，先更新需求，再开发
5. 默认不新增与 MVP 无关的集成和部署形态

---

## 11. 当前总体判断

### 已有基础
- [x] 腾讯云 OCR 已接入
- [x] 坐标排序已有基础
- [x] API 基础已存在
- [x] Express 基础可用
- [x] 网页测试界面已存在

### 仍需补齐的 MVP 关键缺口
- [ ] 统一请求/响应结构
- [ ] 明确并实现 `blocks` 输出
- [ ] 明确 Markdown 输出规范
- [ ] 补齐错误返回规范
- [ ] 把“排版接近原笔记”落成可验收标准
- [ ] 清理非 MVP 项在文档中的干扰

## 结论
**新的 MVP 应聚焦：手写笔记识别 + 手机/网页通用接入 + 排版接近原笔记 + Markdown/blocks 输出。**

其他能力全部延后，不纳入当前 MVP。
